import "../navbar.css";

export default function () {
    return (
        <header className="navbar-section">
            <span className="logo">seraveeem</span>
            <button className="mood-btn">change the mood</button>
            <a href="#contact" className="contact-link"></a>
        </header>
    );
}